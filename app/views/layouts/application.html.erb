<!DOCTYPE html>
<html lang="en">
  <%= render "layouts/header" %>
  <body>
    <%= render "layouts/nav_bar" %>
    <header class="visible-desktop"></header>
    <div class="container">
      <% flash.each do |key, value| %>
        <div class="alert alert-<%= key %>"><%= value %></div>
      <% end %>
      <%= yield %>

    </div> <!-- /container -->
    <%= render "layouts/footer" %>
  </body>
</html>
